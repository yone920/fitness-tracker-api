declare global {
  namespace NodeJS {
    interface ProcessEnv {
      DB_CONNECT: string;
    }
  }
}

// If this file has no import/export statements (i.e. is a script)
// convert it into a module by adding an empty export statement.
export {}

DB_CONNECT = mongodb+srv://yone920:vm3BPso8ES@cluster0.95hdo.mongodb.net/myFirstDatabase?retryWrites=true&w=majority